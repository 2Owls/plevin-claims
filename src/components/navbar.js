import React from 'react';
import { Link } from "gatsby"

const Navbar = () => (
    <nav class="bg-green-800 p-3">
        <div className="container">
            <h2 class="text-gray-50 m-0">My Plevin Claims</h2>
        </div>
    </nav>   
);

export default Navbar;